import {StyleSheet} from "react-native";

export const CustomButtonStyles = StyleSheet.create({
    customButton: {
        borderRadius: 6,
        padding: 15,
        width: '100%'
    },
    customButtonText: {
        textAlign: 'center',
        color: 'white',
        fontSize: 16,
        fontWeight: 'bold'
    }
});